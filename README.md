# cardiorisk-ai
The app trains both models fresh on launch using synthetic data that mirrors the UCI Heart Disease distribution. The XGBoost model produces a feature importance chart showing which of the 13 clinical variables drove the prediction, and the ensemble averages both models for a final risk score with a clear Low / Moderate / High output.
